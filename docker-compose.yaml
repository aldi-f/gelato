version: '3'
services:
  gelato:
    container_name: gelato
    restart: unless-stopped
    build:
      context: .
    volumes:
      - db_data:/data
      # mount /tmp on ram to save read/writes
      - type: tmpfs
        target: /tmp

volumes:
  db_data: